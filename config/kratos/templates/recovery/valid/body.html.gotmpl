<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 20px auto; padding: 30px; border: 1px solid #e0e0e0; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .header { border-bottom: 3px solid #1a237e; padding-bottom: 15px; margin-bottom: 25px; }
        .welcome { font-size: 18px; color: #1a237e; font-weight: bold; }
        .code-container { background-color: #f8f9fa; border: 2px dashed #1a237e; padding: 25px; text-align: center; margin: 30px 0; border-radius: 8px; }
        .recovery-code { font-size: 32px; font-weight: bold; letter-spacing: 8px; color: #1a237e; display: block; }
        .info-text { font-size: 14px; color: #555; }
        .highlight { color: #d32f2f; font-weight: bold; }
        .footer { font-size: 12px; color: #999; margin-top: 40px; border-top: 1px solid #eee; padding-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2 style="margin:0;">IR-Board System</h2>
        </div>
        
        <p class="welcome">Hello {{ .Identity.traits.name }}!</p>
        
        <p>You have been invited to join our platform. To complete your registration and set up your account password, please use the following security code:</p>
        
        <div class="code-container">
            <span class="recovery-code">{{ .RecoveryCode }}</span>
        </div>

        <p class="info-text">This code is for <span class="highlight">one-time use only</span> and expires in <span class="highlight">24 hours</span>.</p>
        
        <p class="info-text">If you were not expecting this invitation or believe it was sent in error, simply ignore this email.</p>
        
        <div class="footer">
            <p>This is an automated message generated by the IR-Board identity management system.<br>
            For your security, never share this code with anyone.</p>
        </div>
    </div>
</body>
</html>